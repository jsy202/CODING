#include <string>
#include <vector>
#include <set>

using namespace std;

set <int> s;
int visited[10];
bool isprime(int n)
{
    if(n < 2) return 0;
    for(int i = 2 ; i * i <= n ; i++)
    {
        if(n%i == 0)return 0;
    }
    return true;
}
void go(string cur, string num)
{
    // 현재 위치에서 아직 사용하지 않은 숫자들을 하나씩 선택
    for(int i = 0; i < num.size(); i++)
    {
        // 이미 현재 경로에서 사용한 숫자 인덱스라면 건너뜀
        if(visited[i]) continue;

        // 선택
        visited[i] = 1;

        // 현재 문자열 뒤에 숫자 하나 추가
        cur += num[i];

        // 지금까지 만든 숫자를 set에 저장
        // set을 쓰는 이유:
        // 같은 숫자가 여러 경로에서 만들어질 수 있기 때문
        // 예: numbers = "011"
        // "11"은 서로 다른 인덱스를 이용해 여러 번 만들어질 수 있음
        s.insert(stoi(cur));

        // 현재까지 만든 문자열 뒤에
        // 또 다른 숫자를 붙이는 경우 탐색
        go(cur, num);

        // 원복
        cur.pop_back();
        visited[i] = 0;
    }
}

int solution(string numbers)
{
    int cnt = 0;

    // 만들 수 있는 모든 숫자를 생성
    go("", numbers);

    // 중복 제거된 숫자들을 하나씩 확인
    for(int x : s)
    {
        if(isprime(x))
            cnt++;
    }

    return cnt;
}


/*
[이 문제에서 배워야 할 핵심]


1. 이 문제는 "숫자를 골라 순서를 바꿔가며 만들 수 있는 모든 수"를 찾는 문제다.

   예를 들어

   numbers = "17"

   이라면 만들 수 있는 수는

   1
   7
   17
   71

   이다.

   즉 단순히 모든 숫자를 한 번씩 쓰는 순열만 보는 게 아니라

   길이 1
   길이 2
   ...
   길이 n

   까지 모두 만들어야 한다.


2. 그래서 이 문제는 "순열형 백트래킹"이다.

   현재 상태에서 해야 할 질문은

   "아직 사용하지 않은 숫자 중
    어떤 숫자를 다음에 붙일 것인가?"

   이다.

   따라서

   for(int i = 0; i < num.size(); i++)

   로 모든 선택지를 확인한다.


3. 이 문제에서는 visited가 필요하다.

   한 숫자 인덱스는
   하나의 숫자를 만드는 과정에서 한 번만 사용할 수 있다.

   예를 들어

   numbers = "123"

   에서

   1 -> 2 -> 1

   처럼 같은 1번 인덱스를 다시 사용하면 안 된다.

   따라서

   if(visited[i]) continue;

   로 중복 사용을 막는다.


4. 중요한 점:
   visited는 "숫자 값"이 아니라 "인덱스"를 방문 처리한다.

   예를 들어

   numbers = "011"

   이면

   num[1] = '1'
   num[2] = '1'

   두 문자는 값은 같지만
   서로 다른 위치에 있는 숫자다.

   따라서 visited[i]는
   각 인덱스를 사용했는지를 기록한다.


5. 백트래킹의 기본 구조는

   선택
   ↓
   재귀
   ↓
   원복

   이다.

   실제 코드에서는

   visited[i] = 1;
   cur += num[i];

   go(cur, num);

   cur.pop_back();
   visited[i] = 0;

   이 구조다.


6. cur.pop_back()이 중요한 이유

   예를 들어

   현재 cur = "1"

   여기서 7을 선택하면

   cur = "17"

   이 된다.

   "17"에서 탐색이 끝난 뒤
   다시 "1" 상태로 돌아가야

   다른 숫자를 붙여볼 수 있다.

   따라서

   cur.pop_back();

   을 해서 방금 추가한 숫자를 제거한다.

   즉

   "재귀 호출 전 상태로 되돌리는 것"

   이 백트래킹이다.


7. 이 문제는 완성된 길이가 n일 때만 저장하면 안 된다.

   예를 들어

   numbers = "17"

   에서 정답 후보는

   1
   7
   17
   71

   이다.

   만약 길이가 2가 됐을 때만 저장하면

   17
   71

   만 검사하게 된다.

   따라서 숫자 하나를 붙일 때마다

   s.insert(stoi(cur));

   를 해야 한다.


8. 이 문제는 명시적인 종료조건이 없어도 된다.

   현재 경로에서 모든 숫자를 사용하면

   for문 안에서

   if(visited[i]) continue;

   에 모두 걸리게 된다.

   그러면 더 이상 재귀 호출이 일어나지 않고
   함수가 자연스럽게 종료된다.


9. 왜 set을 사용하는가?

   같은 숫자가 여러 방법으로 만들어질 수 있기 때문이다.

   예를 들어

   numbers = "011"

   에서는 서로 다른 인덱스의 '1' 때문에

   같은 숫자 11이 여러 탐색 경로에서 만들어질 수 있다.

   하지만 소수 개수를 셀 때는
   11을 한 번만 세어야 한다.

   따라서

   set<int> s;

   를 사용하면 중복 숫자가 자동으로 제거된다.


10. stoi(cur)의 의미

    cur는 문자열이므로

    "17"

    을 실제 정수

    17

    로 바꾸기 위해 stoi를 사용한다.

    stoi("17") -> 17

    stoi("011") -> 11

    즉 앞의 0도 자연스럽게 제거된다.


11. 이 문제의 상태를 정리하면

    cur
    = 현재까지 만든 숫자 문자열

    visited
    = 현재 숫자를 만들 때 이미 사용한 인덱스

    num
    = 원본 숫자 문자열

    s
    = 지금까지 만들 수 있었던 숫자들의 집합


12. 이 문제를 DFS 관점에서 정리하면

    [현재 상태]
    cur = 지금까지 만든 숫자

    [선택]
    아직 사용하지 않은 숫자 하나를 뒤에 붙임

    [선택 처리]
    visited[i] = 1
    cur += num[i]

    [정답 후보 저장]
    s.insert(stoi(cur))

    [다음 상태]
    go(cur, num)

    [원복]
    cur.pop_back()
    visited[i] = 0


13. 앞에서 공부한 문제들과 비교하면

    [부분수열의 합]

    각 원소마다
    선택 / 미선택

    go(idx + 1, ...)
    go(idx + 1, ...)

    visited 필요 X


    [피로도]

    아직 안 간 던전 중
    하나를 선택

    for문 + visited

    선택 -> 재귀 -> 원복


    [소수 찾기]

    아직 안 쓴 숫자 중
    하나를 선택해서 문자열 뒤에 붙임

    for문 + visited

    선택 -> 숫자 생성 -> 재귀 -> 원복


14. 특히 이 문제에서 가져가야 할 중요한 감각

    "순서가 중요하고,
     같은 원소를 한 경로에서 한 번만 사용해야 한다"

    라는 조건이 보이면

    순열형 백트래킹을 의심한다.

    기본 형태는

    for(모든 후보)
    {
        if(이미 사용) continue;

        사용 처리;
        현재 상태 변경;

        dfs();

        현재 상태 원복;
        사용 처리 원복;
    }


[시험장에서 기억할 한 문장]

"아직 안 쓴 것 중 하나를 골라 뒤에 붙인다"
-> for문 + visited + 선택 + 재귀 + 원복

그리고

"중간 길이의 숫자도 정답 후보"
-> 숫자를 하나 붙일 때마다 저장
*/
