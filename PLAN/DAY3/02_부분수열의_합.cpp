#include <bits/stdc++.h>
using namespace std;

int cnt = 0;
vector<int> v;
int n, s;

void go(int idx, int sum)
{
    // 모든 원소에 대해 선택/미선택 결정을 끝낸 경우
    if(idx == n)
    {
        if(sum == s) cnt++;
        return;
    }

    // 현재 원소를 선택하는 경우
    go(idx + 1, sum + v[idx]);

    // 현재 원소를 선택하지 않는 경우
    go(idx + 1, sum);
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> n >> s;

    v.resize(n);

    for(int i = 0; i < n; i++)
        cin >> v[i];

    go(0, 0);

    // s == 0이면 아무것도 선택하지 않은 공집합도
    // 합이 0이라서 cnt에 포함되므로 하나 빼준다.
    if(s == 0) cnt--;

    cout << cnt;

    return 0;
}


/*
[이 문제에서 배워야 할 핵심]

1. DFS를 짜기 전에 "한 단계에서 어떤 선택을 하는가?"를 먼저 생각한다.

   이 문제에서는 각 숫자마다 선택지가 2개이다.

   ① 현재 숫자를 선택한다.
      go(idx + 1, sum + v[idx]);

   ② 현재 숫자를 선택하지 않는다.
      go(idx + 1, sum);

   즉,
   부분집합 / 부분수열 완전탐색
   = 선택한다 / 선택하지 않는다
   = DFS가 2갈래로 뻗는다.


2. 재귀 함수의 매개변수 의미를 명확하게 정한다.

   go(idx, sum)

   idx
   = 지금 선택 여부를 결정해야 하는 원소의 위치

   sum
   = 지금까지 선택한 숫자들의 합

   DFS를 짤 때는 코드를 먼저 쓰지 말고
   "각 매개변수가 무슨 상태를 나타내는가?"
   를 먼저 정의하면 훨씬 쉬워진다.


3. DFS라고 해서 visited가 무조건 필요한 것은 아니다.

   이 문제에서는 항상

   idx -> idx + 1

   방향으로만 이동한다.

   이미 지나간 원소로 다시 돌아갈 일이 없으므로
   visited 배열이 필요 없다.

   visited가 필요한 경우:
   - 그래프 DFS/BFS
   - 미로 탐색
   - 순열처럼 같은 원소를 다시 고를 가능성이 있는 경우

   visited가 보통 필요 없는 경우:
   - 부분집합 선택/미선택
   - idx가 계속 증가하는 조합 탐색


4. 종료조건은
   "언제 하나의 경우가 완성되는가?"
   를 기준으로 잡는다.

   idx == n
   = 모든 숫자를 선택할지 말지 결정한 상태

   따라서 그때 sum == s인지 확인한다.

   if(idx == n)
   {
       if(sum == s) cnt++;
       return;
   }


5. 완전탐색 문제를 만나면 먼저 아래 3개를 생각한다.

   [1] 상태가 무엇인가?
       -> idx, sum

   [2] 현재 상태에서 어떤 선택을 할 수 있는가?
       -> 선택 / 미선택

   [3] 언제 하나의 경우가 완성되는가?
       -> idx == n


6. 이 문제의 DFS 구조를 그림으로 생각하면

                  시작
               /       \
            선택       미선택
           /   \       /   \
        선택  미선택 선택  미선택
                  ...

   숫자가 n개라면 각 숫자마다 선택지가 2개이므로
   총 경우의 수는 2^n이다.


[시험장에서 기억할 한 문장]

부분집합 완전탐색
= 각 원소마다 "넣는다 / 안 넣는다"
= go(idx + 1, ...)
*/
