# CODING 저장소 작업 지침

이 파일은 이 저장소에서 작업하는 모든 Codex 에이전트에게 적용된다.

## 적용 범위

- 사용자가 `확인`이라고 말하거나, `코테 정답.docx`의 내용을 `PLAN`에 반영해 달라고 요청하면 아래의 **정답 아카이브 동기화 절차**를 끝까지 수행한다.
- 사용자가 문맥상 같은 작업을 가리키며 `해줘`라고 말한 경우에도 동일하게 처리한다.
- 일반 알고리즘 풀이 작성이나 기존 코드 수정 요청에는 이 자동 동기화 절차를 적용하지 않는다.

## 작업 목표

`PLAN`은 사용자가 나중에 문제와 풀이를 빠르게 찾아 복습하기 위한 아카이브다. 정답 코드는 원문 그대로 보존하고, 가독성은 Day 폴더, 파일명, 순서, README 목차로 개선한다.

## 원본과 결과 위치

- 읽을 원본: `C:\Users\oie22\OneDrive\Desktop\코테 정답.docx`
- 저장 위치: `PLAN/DAY1`, `PLAN/DAY2`, `PLAN/DAY3`, ...
- 사람용 운영 설명: `PLAN/README.md`
- Day별 목차: `PLAN/DAY숫자/README.md`

## 절대 원칙

1. 원본 DOCX는 읽기 전용으로 취급하며 수정하지 않는다.
2. 정답 코드의 로직, 표현, 주석, 들여쓰기, 공백, 빈 줄을 임의로 고치지 않는다.
3. 오타, 컴파일 오류, 비효율이 보여도 정답 파일에서는 수정하지 않는다. 이 작업의 검증 대상은 정답의 정확성이 아니라 **원문 충실도**다.
4. 긴 학습 주석도 코드의 핵심 복습 자료이므로 생략하거나 요약하지 않는다.
5. 기존 정답 파일을 덮어쓰거나 삭제하지 않는다. 원문 자체가 바뀐 것처럼 보이면 차이를 보고하고 신중히 처리한다.
6. 문제명·플랫폼·번호는 확실할 때만 기록한다. 불확실한 값은 추측하지 말고 `-`로 둔다.
7. 사용자의 다른 변경 사항을 수정하거나 함께 스테이징·커밋하지 않는다.
8. 강제 푸시, `git reset --hard`, 무단 체크아웃, 사용자 변경 사항의 무단 stash를 하지 않는다.

## 정답 아카이브 동기화 절차

사용자가 `확인`이라고 하면 질문 없이 다음을 수행한다.

### 1. 사전 확인

- `git status --short --branch`로 브랜치와 기존 변경 사항을 확인한다.
- DOCX의 존재 여부, 수정 시각, 크기를 확인한다.
- `PLAN/README.md`와 해당 Day의 README를 읽는다.
- 기존 변경 사항이 있으면 작업 대상과 겹치지 않도록 보존한다.

### 2. DOCX 읽기

- 가능하면 Microsoft Word COM을 읽기 전용으로 열어 `Document.Content.Text`를 가져온다.
- Word 텍스트의 `[char]13`은 LF로 바꾸고 문서 끝 표시 `[char]7`은 제거한다.
- `[char]11`은 Word 내부 구간 구분자로 활용할 수 있지만, **구간 하나가 항상 문제 하나라는 가정은 하지 않는다.**
- Word COM을 사용할 수 없으면 DOCX ZIP 내부의 `word/document.xml`을 읽는 방식으로 대체한다. 원본을 수정하거나 재저장하지 않는다.

### 3. Day와 문제 경계 판별

- `DAY2`, `day3`처럼 대소문자가 다른 Day 표기도 동일하게 인식한다.
- 코드 앞의 일반 텍스트가 문제명으로 보이면 파일명과 README에 사용한다.
- `#include`, `main`, `solution`, 전역 변수·함수 이름을 살펴 문제 경계를 논리적으로 판단한다.
- 새 코드와 기존 파일의 내용을 비교해 이미 저장된 문제는 다시 만들지 않는다.
- 동일한 코드가 DOCX에 중복되어 있으면 한 파일만 보관하고 Day README에 중복 사실을 기록한다.
- Word 내부 저장 순서 때문에 한 문제의 코드가 여러 구간으로 나뉘어 있으면, 다른 문제 코드를 섞지 말고 원래 조각의 문자 내용을 그대로 이어 붙인다. 이때 코드 자체를 새로 작성하거나 정리하지 않는다.

### 4. 파일과 README 작성

- Day별 순서는 `01`부터 다시 시작한다.
- 파일명 형식은 `두자리순서_문제명.cpp`로 한다.
- 예: `PLAN/DAY2/09_순서대로_방문하기.cpp`
- 새 Day가 나타나면 해당 폴더와 `README.md`를 만든다.
- README 표 형식은 기존 Day README를 그대로 따른다.
- README에는 순서, 플랫폼, 문제 번호, 문제명, 정답 파일의 상대 링크를 넣는다.
- 가독성을 위한 설명은 README에만 작성하며 `.cpp` 원문에는 추가하지 않는다.

### 5. 원문 대조

- 모든 기존 파일과 새 파일을 DOCX 원문에 다시 대응시킨다.
- 비교 시 CRLF와 LF 차이만 정규화할 수 있다.
- 내부 공백, 들여쓰기, 빈 줄, 주석은 정규화하거나 무시하지 않는다.
- 새 파일마다 문자 단위 일치 여부를 확인하고, 하나라도 다르면 커밋 전에 수정한다.
- 중복 코드나 분할 코드처럼 단순 1:1 대응이 아닌 경우에는 원문 조각별 비교 결과와 재결합 결과를 별도로 확인한다.

### 6. Git 반영

- `git fetch origin`으로 원격 상태를 확인한다.
- 이번에 만든 정답 파일과 해당 README만 경로를 명시해 `git add`한다.
- 커밋 전에 `git diff --cached --stat`과 스테이징 파일 목록을 확인한다.
- 의미가 드러나는 영어 커밋 메시지를 사용한다. 예: `docs: add Day 3 brute force solutions`.
- `git rebase origin/main`으로 원격 최신 커밋 위에 재배치하고 `git push origin main`으로 푸시한다.
- 충돌이 나면 사용자 파일을 버리거나 강제 푸시하지 않는다. 안전하게 해결할 수 없는 충돌은 정확히 보고한다.

### 7. 완료 검증과 보고

- `git status --short --branch`가 깨끗하고 `main...origin/main`에 ahead/behind 표시가 없는지 확인한다.
- `git log -1`에서 실제 최종 커밋 해시를 다시 읽는다. 추측하거나 이전 해시를 보고하지 않는다.
- 최종 답변에는 추가된 Day·문제, 원문 대조 결과, README 갱신, 실제 커밋 해시, 푸시·동기화 상태를 간단히 적는다.

## 현재 DOCX의 알려진 특이사항

- `DAY3`/`day3` 표기가 섞여 있다.
- 백준 2231 `분해합` 코드가 동일한 내용으로 두 번 들어 있다. `PLAN/DAY3/01_분해합.cpp` 하나만 유지한다.
- 프로그래머스 42839 `소수 찾기`는 Word 내부에서 앞부분과 뒷부분이 나뉘고, 그 사이에 프로그래머스 87946 `피로도` 코드가 위치한다.
  - `PLAN/DAY3/03_피로도.cpp`에는 피로도 코드만 둔다.
  - `PLAN/DAY3/04_소수_찾기.cpp`에는 소수 찾기의 앞·뒤 원문 조각만 순서대로 연결한다.
- 이 특이사항은 향후 DOCX가 정리될 수 있으므로 매번 현재 문서를 다시 확인한다.

## 완료 조건

아래 조건을 모두 만족해야 `확인` 작업이 끝난 것이다.

- 새 정답을 빠짐없이 문제별 파일로 저장함
- 코드와 학습 주석을 원문 그대로 보존함
- Day README에서 각 파일을 바로 열 수 있음
- 중복·분할 코드가 복습하기 좋은 형태로 정리됨
- 이번 작업 파일만 커밋함
- GitHub `main`에 푸시함
- 작업 트리가 깨끗하고 로컬 `main`과 `origin/main`이 동기화됨
